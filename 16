import React from 'react';
import { Database, Cpu, TestTube2, BarChartHorizontal } from 'lucide-react';

const StatCard = ({ icon, title, value, description }) => {
  const Icon = icon;
  return (
    <div className="bg-gray-800/50 border border-gray-700 rounded-xl p-6 flex items-start space-x-4">
      <div className="bg-gray-700 p-3 rounded-lg">
        <Icon className="h-6 w-6 text-cyan-400" />
      </div>
      <div>
        <p className="text-sm text-gray-400">{title}</p>
        <p className="text-2xl font-bold text-white">{value}</p>
        <p className="text-xs text-gray-500">{description}</p>
      </div>
    </div>
  );
};

const DashboardPage = () => {
  return (
    <div>
      <h1 className="text-3xl font-bold text-white mb-2">Selamat Datang Kembali!</h1>
      <p className="text-gray-400 mb-8">Berikut ringkasan aktivitas machine learning Anda.</p>
      
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        <StatCard icon={Database} title="Dataset Dianalisis" value="14" description="+2 minggu ini" />
        <StatCard icon={Cpu} title="Model Dilatih" value="32" description="Regresi Linier populer" />
        <StatCard icon={TestTube2} title="Eksperimen Dijalankan" value="128" description="7 eksperimen aktif" />
        <StatCard icon={BarChartHorizontal} title="Rata-rata Akurasi" value="92.1%" description="Naik 1.5% dari bulan lalu" />
      </div>

      <div className="mt-10 bg-gray-800/50 border border-gray-700 rounded-xl p-6">
        <h2 className="text-xl font-semibold text-white mb-4">Aktivitas Terbaru</h2>
        <p className="text-gray-400">Umpan aktivitas akan ditampilkan di sini.</p>
      </div>
    </div>
  );
};

export default DashboardPage;
